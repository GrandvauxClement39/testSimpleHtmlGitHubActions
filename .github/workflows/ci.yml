name: gitHubActionSimpleHtml

on:
  push:
    branches:
      - 'master'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create archive of index.html
        run: tar cfz website.tgz ./